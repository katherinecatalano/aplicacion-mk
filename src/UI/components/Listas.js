import React, { Fragment } from "react";


export default function Listas (){
    return(
    <Fragment>
        const options = [
            'one', 'two', 'three'
        ];


        const defaultOption = option[0];
        <Dropdown options= { options } onChange={this._onSelect} value= {defaultOption} placeholder="Seleccione una opcion" />
        </Fragment>
    )
}
    
